{
	"name": "stream-kit-plugin",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"license": "MIT",
	"scripts": {
		"build": "vite build --config vite.build.config.js",
		"check": "tsc --noEmit",
		"dev": "vite build --config vite.build.config.js --watch",
		"package": "pnpm build && node scripts/package.mjs"
	},
	"dependencies": {
		"@stream-kit/core": "^{{SDK_CORE_VERSION}}"
	},
	"devDependencies": {
		"@stream-kit/plugin": "^{{SDK_PLUGIN_VERSION}}",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"svelte": "^5.56.1",
		"typescript": "~6.0.3",
		"vite": "^8.0.16",
		"vite-plugin-node-polyfills": "^0.28.0"
	}
}
