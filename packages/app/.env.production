# Baked into release / `tauri build` (overrides packages/app/.env).
# Keep these on production hosts — never localhost.
PUBLIC_POCKETBASE_URL=https://api.stream-kit.app
PUBLIC_SITE_URL=https://stream-kit.app
